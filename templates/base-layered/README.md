# {{ project_name }}

Generated by Forge.

## Run locally

    docker compose up -d
    mvn spring-boot:run

## Database credentials

The default Postgres credentials (`forge`/`forge`) are hardcoded in
`docker-compose.yml` and `src/main/resources/application.yml` for local
development convenience. Change them before this project is used anywhere
beyond a local dev machine (shared environment, staging, production, or
anywhere the repo/image might be exposed).
