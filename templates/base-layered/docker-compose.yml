services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: {{ artifact_id.replace('-', '_') }}
      POSTGRES_USER: forge
      POSTGRES_PASSWORD: forge
      TZ: UTC
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
