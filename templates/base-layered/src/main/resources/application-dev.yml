spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/{{ artifact_id.replace('-', '_') }}
    username: forge
    password: forge
  jpa:
    show-sql: true
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

springdoc:
  swagger-ui:
    enabled: true
